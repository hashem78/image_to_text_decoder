name: image_to_text_decoder
description: A project to help with cheating

publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=2.12.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter
  clipboard: ^0.1.3
  cupertino_icons: ^1.0.2
  ffi: ^1.0.0
  file_selector: ^0.8.2
  file_selector_windows: ^0.0.2
  flutter_bloc: ^7.0.0
  flutter_hooks: ^0.17.0
  http: ^0.13.3
  path_provider: ^2.0.1
  shared_preferences: ^2.0.5
  watcher: ^1.0.0
  win32: ^2.0.5
  window_size:
    git:
      url: git://github.com/google/flutter-desktop-embedding.git
      path: plugins/window_size

dev_dependencies:
  flutter_test:
    sdk: flutter

  flutter_lints: ^1.0.0

flutter:
  uses-material-design: true
